---
// Primer · For newcomers. Sets up *why* AI agents are needed for
// specialized scientific work before the rest of the landing page
// dives into AKD specifics. Sourced verbatim-ish from front.md and
// tightened for the page voice.
---
<section class="section section--primer" id="primer">
  <div class="container">
    <div class="section-head">
      <h1>Accelerated Knowledge Discovery: AI Agents for Science</h1>
    </div>

    <div class="primer-rows">
      <article class="primer-row">
        <div class="primer-row__lead">
          <span class="primer-row__num">01</span>
          <h3>Why Standard AI Chatbots Struggle with Specialized Science Tasks</h3>
        </div>
        <div class="primer-row__body">
          <p>Standard AI chatbots often lack access to the specialized tools, databases, terminology, and ontologies needed for advanced scientific work. As a result, they have a higher risk of hallucinating when they operate in narrow technical domains.</p>
          <p>The value a chatbot provides usually depends on the user&rsquo;s domain expertise. A subject matter expert can ask precise questions, recognize weak reasoning, and quickly judge whether an answer is correct. A novice, however, may not be sure what to ask, which assumptions to challenge, or how to verify a response.</p>
          <p>This uneven performance is often described as the <a class="jagged-link" href="https://www.oneusefulthing.org/p/centaurs-and-cyborgs-on-the-jagged" target="_blank" rel="noopener">&ldquo;jagged edge of AI&rdquo;</a>: AI performs well on some tasks but fails unexpectedly on others, even when the tasks appear similar.</p>
        </div>
      </article>

      <article class="primer-row">
        <div class="primer-row__lead">
          <span class="primer-row__num">02</span>
          <h3>What Is an AI Agent?</h3>
        </div>
        <div class="primer-row__body">
          <p>An AI agent can be viewed as an independent digital worker. You give it a task, and it determines how to accomplish that task.</p>
          <p>Unlike a standard chatbot, which mainly responds to questions, an agent can take action. It can analyze a problem, break it into smaller steps, use the right tools, review intermediate results, and correct mistakes as it works toward completion.</p>
        </div>
      </article>

      <article class="primer-row">
        <div class="primer-row__lead">
          <span class="primer-row__num">03</span>
          <h3>How an Agent Is Designed</h3>
        </div>
        <div class="primer-row__body">
          <p>A well-designed agent typically includes three core elements:</p>
          <ul class="primer-elements" aria-label="Three core elements of a well-designed agent">
            <li><b>Reasoning logic</b>, often implemented through prompts or instructions.</li>
            <li><b>Contextual information</b>, which grounds the agent&rsquo;s reasoning in the relevant domain.</li>
            <li><b>Tool access</b>, along with clear guidance on when to use each tool and how to use it correctly.</li>
          </ul>
          <p>Through the design of these elements, agents can help smooth the jagged edge of AI. When a subject matter expert helps build the agent, the design can capture some of that expert&rsquo;s implicit knowledge: how to reason through the task, what context matters, which tools to use, and when to use them.</p>
          <p>As a result, a novice using the agent may achieve productivity gains closer to those of an expert because the agent embeds expert judgment into the workflow.</p>
          <p class="primer-row__caveat">However, designing an agent for complex scientific tasks is difficult, and it should not be treated as a purely technical endeavor. Building a capable agent requires close collaboration among developers, scientists, domain experts, and end users to ensure the agent reasons correctly, uses tools appropriately, and produces reliable outputs.</p>
        </div>
      </article>
    </div>
  </div>
</section>

<style>
  .section--primer {
    /* Compact landing-page section: title + three rows fit inside one
       viewport on a 1080p desktop. */
    padding: 36px 0 28px;
  }
  .section--primer .section-head {
    margin-bottom: 20px;
  }
  .section--primer h1 {
    font-family: var(--font-serif);
    font-weight: 300;
    font-size: clamp(32px, 5vw, 56px);
    line-height: 1.1;
    letter-spacing: -0.02em;
    margin: 12px 0 16px;
    color: var(--text);
    text-wrap: balance;
  }

  .primer-rows {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
    margin-top: 4px;
  }

  .primer-row {
    display: flex;
    flex-direction: column;
    gap: 8px;
    padding: 16px 18px;
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    background: var(--bg-0);
    box-shadow: 0 1px 2px rgba(0,0,0,0.04);
  }

  .primer-row__lead {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }
  .primer-row__num {
    font-family: var(--font-mono);
    font-size: 22px;
    line-height: 1;
    font-weight: 500;
    color: var(--accent);
    letter-spacing: -0.01em;
  }
  .primer-row__lead h3 {
    font-family: var(--font-serif);
    font-weight: 600;
    font-size: 18px;
    line-height: 1.2;
    margin: 2px 0 0;
    color: var(--text);
    text-wrap: balance;
  }
  .primer-row__hint {
    font-family: var(--font-mono);
    font-size: 10.5px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--text-muted);
    margin: 0;
  }

  .primer-row__body {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .primer-row__body p {
    font-size: 13.5px;
    line-height: 1.5;
    color: var(--text-dim);
    margin: 0;
  }
  .primer-row__body em {
    font-style: italic;
    color: var(--text);
  }
  .primer-row__caveat {
    padding-left: 10px;
    border-left: 2px solid var(--accent);
    font-size: 12.5px !important;
  }

  /* "Jagged edge of AI" citation link: italic phrase, accent color,
     dotted underline so it reads as a referenced term rather than
     a generic link. */
  .jagged-link {
    color: var(--accent);
    font-style: italic;
    font-weight: 500;
    text-decoration: underline dotted;
    text-underline-offset: 3px;
    transition: color 0.15s ease;
  }
  .jagged-link:hover {
    color: var(--accent-4);
    text-decoration-style: solid;
  }

  /* Three-element list inside row 03: inline-text items, bordered. */
  .primer-elements {
    list-style: none;
    padding: 0;
    margin: 2px 0 4px;
    display: flex;
    flex-direction: column;
    gap: 6px;
  }
  .primer-elements li {
    border-left: 2px solid var(--border-strong);
    padding: 2px 0 2px 10px;
    font-size: 13px;
    line-height: 1.45;
    color: var(--text-dim);
  }
  .primer-elements b {
    font-family: var(--font-serif);
    font-weight: 600;
    color: var(--text);
  }

  /* ── Responsive ───────────────────────────────────────── */
  @media (max-width: 900px) {
    .primer-rows { grid-template-columns: 1fr; }
  }
</style>
